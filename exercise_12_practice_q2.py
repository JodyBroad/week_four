# string data, so will count the individual characters
tup = 'Hello'
print(len(tup))
print(type(tup))

# because of the , after the string, this is setting the data type to be tuple, with only 1 item in it, hence len = 1
tup = "Hello",
print(len(tup))
print(type(tup))